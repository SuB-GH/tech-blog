<article class="post">
  <div class="title">
    <a href="/post/{{id}}">{{title}}</a>      
    <span>({{title}} {{post.post_contents}})</span>  
       <a>{{post_contents}}</a>
        <span>({{post.post-contents}})</span>
  </div>
  <div>{{title}}</div>

  <div class="meta">
    {{post.post_contents}}
    Posted by {{user.username}} on {{created_at}}
    |
    {{!-- <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a> --}}
     {{!-- <a href="/post/{{id}}">{{comments.length}}</a> --}}
<a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
  
  </div>
</article>

{{!-- <article class="post">
  <div class="title">
    <a href="{{title}}" target="_blank">{{title}}</a>
    <span>({{title}})</span>
  </div>
  <div class="meta">
    
    Posted by {{user.username}} on {{format_date created_at}}
    |
    <a href="/post/{{id}}">{{comments.length}} comment(s)</a>
  </div>
</article> --}}